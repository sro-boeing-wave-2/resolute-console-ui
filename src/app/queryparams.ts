export interface queryParams {
  status: string;
  source: string;
  priority: string;
  page: number;
  sortBy: string;
  sortOrder: boolean; //false = ascending; true = descending;
}

// export enum sortOrder {
//   ascending = 0,
//   descending = 1
// }
